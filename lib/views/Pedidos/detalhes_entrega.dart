import 'package:flutter/material.dart';

class DetalhesEntrega extends StatelessWidget {
  const DetalhesEntrega({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
